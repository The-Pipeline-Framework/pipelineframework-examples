/**
 * Consumer application compilation anchor for the packaged callable-loop proof.
 *
 * <p>All computational topology and state evolution live in the imported Block; this distinct
 * package exists only so normal Java compilation invokes TPF's application compiler.</p>
 */
package org.pipelineframework.examples.callableloopapp;
